<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="card">
    <div class="card-body">
      <%= f.input :name %>
      <%= f.input :email, input_html: { autocomplete: "email" }%>
      <%= f.input :password, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length), label: "Password (Note: changing this field will sign you out if you are editing you're own profile)" %>
      <%= f.input :admin %>
    </div>

    <div class="card-footer">
      <div class="card-footer-btn-group">
        <%= f.button :submit %>
        <%= yield %>
      </div>
    </div>
  </div>
<% end %>
