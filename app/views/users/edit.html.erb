<%= content_for :title do %>
  Edit User • <%= @user.name %>
<% end %>

<div class="card">
  <div class="card-body">
    <h1 class="text-center">Editing user</h1>
  </div>
</div>

<%= render "form", user: @user do %>
  <%= link_to "Discard changes", @user, data: { turbo_method: :get, turbo_confirm: 'Are you sure?' }, class: 'btn' %>
<% end %>
