<%= content_for :title do %>
  New Page
<% end %>

<div class="card">
  <div class="card-body">
    <h1>New page</h1>
  </div>
</div>

<%= render "form", page: @page do %>
  <%= link_to "Discard changes", pages_path, data: { turbo_method: :get, turbo_confirm: 'Are you sure?' }, class: 'btn' %>
<% end %>
